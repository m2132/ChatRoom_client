import { useDispatch } from "react-redux";
import {AddRoom} from "./RoomSlice";


const AddRoom=()=>{
    let dispatch=useDispatch();

    let empty={
        name:"",
        description:"",
        users:"",
        createdAt:""
    }

    const change=(e)=>{
        let {}
    }
}